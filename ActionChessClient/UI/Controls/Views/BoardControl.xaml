<UserControl x:Class="ActionChessClient.BoardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             x:ClassModifier="internal">
    <Canvas
        Name="BoardCanvas"
        Width="{DynamicResource BoardSize}"
        Height="{DynamicResource BoardSize}"
        AllowDrop="True"
        Background="Transparent">
        <Image Name="Background" Source="/Images\board.png" MaxWidth="{DynamicResource BoardSize}" MaxHeight="{DynamicResource BoardSize}"/>
    </Canvas>
</UserControl>
